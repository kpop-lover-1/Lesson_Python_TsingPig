当然可以！以下是一份**可直接运行、逐段练习的 Python 代码合集**，内容完全基于你提供的笔记，并做了清晰分段、注释和可执行封装。你可以**一段一段复制到 Python 环境中运行**，边敲边理解。

---

# ✅ Python 进阶语法 & 数据结构练习代码（含完整注释）

> 💡 建议：使用 Jupyter Notebook 或 Python 脚本逐块运行，观察输出结果

```
# ==============================
# 1. 列表推导式（List Comprehension）
# ==============================

print("【1】列表推导式示例")
# 示例1：1~10 的平方
squares = [i**2 for i in range(1, 11)]
print("1~10 的平方:", squares)

# 示例2：1~20 的偶数
evens = [i for i in range(1, 21) if i % 2 == 0]
print("1~20 的偶数:", evens)

# 示例3：输入字符串转整数列表（模拟输入）
input_str = "1 2 3 4 5"
input_data = [int(x) for x in input_str.split()]
print("字符串转整数列表:", input_data)

# 示例4：n×n 零矩阵（正确写法）
n = 3
zero_matrix = [[0 for _ in range(n)] for _ in range(n)]
print(f"{n}x{n} 零矩阵:", zero_matrix)

# 错误示范（浅拷贝）—— 不要这样写！
# bad_matrix = [[0] * n] * n
# bad_matrix[0][0] = 999  # 会修改所有行！

# 示例5：斐波那契数列（n=10）
n = 10
fib = [0, 1]
[fib.append(fib[-1] + fib[-2]) for _ in range(n - 2)]
print("斐波那契数列 (n=10):", fib[:n])

# 示例6：展平二维列表
matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
flatten = [x for row in matrix for x in row]
print("展平二维列表:", flatten)

# 示例7：字符计数（注意效率问题）
s = "algorithm"
char_count = {char: s.count(char) for char in set(s)}
print("字符出现次数（字典推导）:", char_count)

# 更高效方式（推荐）
from collections import Counter
char_count_fast = dict(Counter(s))
print("字符出现次数（Counter）:", char_count_fast)


# ==============================
# 2. range() 函数详解
# ==============================

print("\n【2】range() 函数示例")

# 基础用法
print("range(10):", list(range(10)))

# 奇数序列
print("1~10 的奇数:", list(range(1, 11, 2)))

# 倒序
print("10 到 1 倒序:", list(range(10, 0, -1)))

# 循环索引（模运算）
n = 5
indices = [i % n for i in range(n * 2)]
print("周期性索引 (n=5):", indices)

# 生成网格坐标
rows, cols = 2, 3
grid = [(i, j) for i in range(rows) for j in range(cols)]
print("2x3 网格坐标:", grid)


# ==============================
# 3. 埃拉托斯特尼筛法（素数筛）
# ==============================

print("\n【3】埃氏筛求素数")

import math

def sieve_of_eratosthenes(n):
    is_prime = [True] * (n + 1)
    is_prime[0] = is_prime[1] = False
    for i in range(2, int(math.sqrt(n)) + 1):
        if is_prime[i]:
            for j in range(i * i, n + 1, i):
                is_prime[j] = False
    return [i for i in range(2, n + 1) if is_prime[i]]

primes_30 = sieve_of_eratosthenes(30)
print("≤30 的素数:", primes_30)


# ==============================
# 4. 栈与队列操作
# ==============================

print("\n【4】栈与队列操作")

# 栈（用 list）
stack = []
stack.append(1)
stack.append(2)
print("入栈后:", stack)
top = stack[-1]
popped = stack.pop()
print("栈顶:", top, "弹出:", popped, "当前栈:", stack)

# 队列（用 deque）
from collections import deque

q = deque()
q.append(10)
q.append(20)
print("入队后:", list(q))
front = q.popleft()
print("出队元素:", front, "当前队列:", list(q))

# 双端操作
q.appendleft(5)
print("左侧入队 5 后:", list(q))


# ==============================
# 5. 逆波兰表达式（后缀表达式）求值
# ==============================

print("\n【5】逆波兰表达式求值")

def eval_rpn(tokens):
    stack = []
    ops = {
        '+': lambda a, b: a + b,
        '-': lambda a, b: a - b,
        '*': lambda a, b: a * b,
        '/': lambda a, b: int(a / b)  # 向零取整
    }
    for t in tokens:
        if t in ops:
            b = stack.pop()
            a = stack.pop()
            stack.append(ops[t](a, b))
        else:
            stack.append(int(t))
    return stack[0]

# 测试用例
expr1 = ["2", "1", "+", "3", "*"]      # (2+1)*3 = 9
expr2 = ["4", "13", "5", "/", "+"]     # 4 + (13/5) = 4 + 2 = 6

print("RPN 表达式1 结果:", eval_rpn(expr1))
print("RPN 表达式2 结果:", eval_rpn(expr2))


# ==============================
# 6. 螺旋矩阵生成（进阶）
# ==============================

print("\n【6】螺旋矩阵生成")

def generate_spiral_matrix(n):
    matrix = [[0] * n for _ in range(n)]
    directions = [(0, 1), (1, 0), (0, -1), (-1, 0)]  # 右、下、左、上
    x, y, num, d = 0, 0, 1, 0

    for step in range(n, 0, -2):
        loops = 4 if step > 1 else 1
        for _ in range(loops):
            dx, dy = directions[d]
            for _ in range(step - 1):
                matrix[x][y] = num
                num += 1
                x += dx
                y += dy
            d = (d + 1) % 4

    if n % 2 == 1:
        matrix[n//2][n//2] = num

    return matrix

spiral_3 = generate_spiral_matrix(3)
print("3x3 螺旋矩阵:")
for row in spiral_3:
    print(row)

spiral_4 = generate_spiral_matrix(4)
print("\n4x4 螺旋矩阵:")
for row in spiral_4:
    print(row)
```

---

## ✅ 练习建议：

1. **逐段运行**：从第1部分开始，每段复制到你的 IDE 或 REPL 中运行。
2. **修改参数**：比如把 `n=3` 改成 `n=5`，看矩阵变化。
3. **尝试错误**：故意写错（如用 `[[0]*n]*n`），观察 bug。
4. **扩展功能**：
    - 给 `eval_rpn` 加异常处理
    - 用栈实现括号匹配
    - 用队列实现 BFS 遍历二叉树